const $=s=>document.querySelector(s);let requests=[],products=[],timer=null;const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));async function api(path,opt={}){const r=await fetch(path,{...opt,headers:{'content-type':'application/json',...(opt.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Error');return d}function state(s){return s==='En revisión'?'<span class="state review">En revisión</span>':s==='Aprobado'?'<span class="state approved">Aprobado</span>':s==='Entregado'?'<span class="state delivered">Entregado</span>':'<span class="state rejected">Rechazado</span>'}function details(lines){if(!lines?.length)return'—';const groups={};for(const l of lines){const k=l.ruc+'|'+l.company;(groups[k]??=[]).push(l)}return Object.entries(groups).map(([k,ls])=>{const [ruc,company]=k.split('|');return `<b>${esc(company)}</b> (${esc(ruc)})<br>${ls.map(x=>`${esc(x.model)}: ${x.qty}`).join(' · ')}`}).join('<hr style="border:0;border-top:1px solid #eee">')}function filtered(){const q=($('#search')?.value||'').trim().toLowerCase(),status=$('#statusFilter')?.value||'Todos';return requests.filter(r=>{if(status!=='Todos'&&r.status!==status)return false;if(!q)return true;const hay=[r.executive,r.channel,r.product_name,r.status,r.comment,r.admin_note,...(r.lines||[]).flatMap(x=>[x.ruc,x.company,x.model,x.qty])].join(' ').toLowerCase();return hay.includes(q)})}async function load(){const [rq,pr]=await Promise.all([api('/api/admin/requests'),api('/api/admin/products')]);requests=rq.requests||[];products=pr.products||[];render();$('#lastSync').textContent='Última actualización: '+new Date().toLocaleTimeString('es-PE')}function render(){$('#kTotal').textContent=requests.length;$('#kUnits').textContent=requests.reduce((s,x)=>s+Number(x.total_qty||0),0);$('#kReview').textContent=requests.filter(x=>x.status==='En revisión').length;$('#kApproved').textContent=requests.filter(x=>x.status==='Aprobado').length;$('#kDelivered').textContent=requests.filter(x=>x.status==='Entregado').length;$('#kRejected').textContent=requests.filter(x=>x.status==='Rechazado').length;const view=filtered(),rows=$('#rows');rows.innerHTML='';if(!view.length)rows.innerHTML='<tr><td colspan="10" style="text-align:center;color:#667085;padding:24px">No hay solicitudes que coincidan con el filtro.</td></tr>';for(const r of view){let acts='—';if(r.status==='En revisión')acts=`<button class="btn success" data-act="Aprobado" data-id="${r.id}">Aprobar</button> <button class="btn danger" data-act="Rechazado" data-id="${r.id}">Rechazar</button>`;else if(r.status==='Aprobado')acts=`<button class="btn" data-act="Entregado" data-id="${r.id}">Entregado</button> <button class="btn danger" data-act="Rechazado" data-id="${r.id}">Anular</button>`;rows.insertAdjacentHTML('beforeend',`<tr><td>${new Date(r.created_at).toLocaleString('es-PE')}</td><td>${esc(r.executive)}</td><td>${esc(r.channel)}</td><td>${esc(r.product_name)}</td><td><b>${r.total_qty}</b></td><td>${details(r.lines)}</td><td>${esc(r.comment||'—')}</td><td>${state(r.status)}</td><td>${esc(r.admin_note||'—')}</td><td>${acts}</td></tr>`)}const inv=$('#inventory');inv.innerHTML='';for(const p of products)inv.insertAdjacentHTML('beforeend',`<div class="inv"><h3>${esc(p.name)}</h3><div style="font-size:13px;color:#475467;margin-bottom:9px">Disponible: <b>${p.available}</b> · Reservado: <b>${p.reserved}</b> · Entregado: <b>${p.delivered}</b></div><div class="invgrid"><label class="field">Stock total<input type="number" min="0" value="${p.total_stock}" data-stock="${p.id}"></label><label class="field">Cuota<input type="number" min="1" value="${p.quota}" data-quota="${p.id}"></label></div><button class="btn primary" data-save="${p.id}" style="margin-top:10px">Guardar</button></div>`)}$('#adminView').classList.remove('hidden');$('#loginView').classList.add('hidden')}function startTimer(){clearInterval(timer);timer=setInterval(()=>load().catch(()=>{}),30000)}async function boot(){try{const h=await api('/api/health');if(!h.databaseConfigured){$('#loginError').textContent='Falta vincular la base de datos D1 en Cloudflare.';return}if(!h.adminPasswordConfigured){$('#loginError').textContent='Falta configurar la contraseña de administrador en Cloudflare.';return}await api('/api/admin/me');await load();startTimer()}catch{}}$('#loginBtn').onclick=async()=>{try{$('#loginError').textContent='';await api('/api/admin/login',{method:'POST',body:JSON.stringify({password:$('#password').value})});await load();startTimer()}catch(e){$('#loginError').textContent=e.message}};$('#password').addEventListener('keydown',e=>{if(e.key==='Enter')$('#loginBtn').click()});$('#search').addEventListener('input',render);$('#statusFilter').addEventListener('change',render);$('#refreshBtn').onclick=()=>load().catch(e=>alert(e.message));$('#logoutBtn').onclick=async()=>{clearInterval(timer);await api('/api/admin/logout',{method:'POST'}).catch(()=>{});location.reload()};$('#rows').addEventListener('click',async e=>{const b=e.target.closest('[data-act]');if(!b)return;const label=b.dataset.act==='Rechazado'?'Motivo / observación del rechazo:':'Observación (opcional):',note=prompt(label,'');if(note===null)return;try{await api('/api/admin/status',{method:'POST',body:JSON.stringify({id:b.dataset.id,status:b.dataset.act,note})});await load()}catch(err){alert(err.message)}});$('#inventory').addEventListener('click',async e=>{const b=e.target.closest('[data-save]');if(!b)return;const id=b.dataset.save,stock=$(`[data-stock="${id}"]`),quota=$(`[data-quota="${id}"]`);try{await api('/api/admin/product',{method:'POST',body:JSON.stringify({id,total_stock:Number(stock.value),quota:Number(quota.value)})});await load()}catch(err){alert(err.message)}});$('#exportBtn').onclick=()=>{const rows=[['Fecha','Ejecutivo','Canal','Premio','Unidades reportadas','Estado','Comentario FFVV','Observación admin','RUC/RUT','Razón social','Modelo','Cantidad']];for(const r of requests)for(const l of(r.lines||[]))rows.push([r.created_at,r.executive,r.channel,r.product_name,r.total_qty,r.status,r.comment||'',r.admin_note||'',l.ruc,l.company,l.model,l.qty]);const csv=rows.map(row=>row.map(v=>`"${String(v??'').replaceAll('"','""')}"`).join(',')).join('\n'),blob=new Blob(['\ufeff'+csv],{type:'text/csv;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='HONOR_Rewards_solicitudes.csv';a.click();URL.revokeObjectURL(url)};boot();